# include <iostream>
// gloval variable in C++ is set to 0 automatically
// static value retain the value after multiple time function call
int main(){
    long long res = 0
    for (int i = 1, i <= n, i ++){
        int x = 1e5
        for (int j = i, j <= n, j ++){
            x - min(x, a[j])
            res = max(res, (j-i+1) * x)
        }
    }
}